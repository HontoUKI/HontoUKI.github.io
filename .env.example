# Sensitive contact data — copy to .env (gitignored) for local dev.
# On GitHub Pages these are injected at build time from repo Variables
# (Settings → Secrets and variables → Actions → Variables). Leave empty to hide.
VITE_CONTACT_EMAIL=
VITE_CONTACT_PHONE=
